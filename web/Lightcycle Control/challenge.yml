# This file represents the base specification of your challenge. It is used by
# other tools to install and deploy your challenge.

# Required sections
name: "Lightcycle Control"
attribution: "Written by Aman"
category: Web 
connection_info: http://107.191.48.172:8002
description: As you drive around in your lightcycle, a rogue program take the control away from you! Can you figure out what is happening on it's control panel and get the control back (and the flag)?
value: 100
type: standard

flags:
    # A static case sensitive flag
    - dalctf{r34l_d3v_0v3rr1d3}
    - {
        type: "static",
        content: "dalctf{r34l_d3v_0v3rr1d3}",
        data: "case_insensitive",
    }

# Hints are used to give players a way to buy or have suggestions. They are not
# required but can be nice.
# Can be removed if unused
# Accepts dictionaries or strings

# If the field is omitted, the challenge is visible by default.
# If provided, the field can take one of two values: hidden, visible.
state: visible

# Specifies what version of the challenge specification was used.
# Subject to change until ctfcli v1.0.0
version: "0.1"
